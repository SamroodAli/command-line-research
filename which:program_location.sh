# which takes in the name of a program/executable as argument and shows you 
# the past where the executable resides

which ls # /bin/ls -- this is where the ls program resides

# This will fail for shell built in commands such as pwd

which pwd # fails as pwd is a shell built-in command