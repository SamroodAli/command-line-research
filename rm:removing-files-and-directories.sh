# we can remove files with rm
rm file1 file2

# we can use regex

rm fil* # deletes all files starting with fil

# delete directories with rm -rf
# -r recursively
# -f delete even if a file is not empty 

rm -rf  # delete directories recursively

rm -rf directory